import React from 'react';
import HeroCarousel from "./Carousel.tsx";

const Hero: React.FC = () => {
    return (
        <div className="w-full h-full">
            <HeroCarousel/>
        </div>
    );
};
export default Hero;
